---
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: app2
  name: deployment-app2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-app2
  template:
    metadata:
      labels:
        app: app-app2
      annotations:
        vault.hashicorp.com/agent-inject: 'true'
        vault.hashicorp.com/agent-cache-enable: "true"
        vault.hashicorp.com/ca-cert: "/var/run/secrets/kubernetes.io/serviceaccount/ca.crt"
        vault.hashicorp.com/role: 'app2'
        vault.hashicorp.com/agent-inject-secret-config: 'secret/data/services/app2/config'
        vault.hashicorp.com/agent-inject-template-config: |
          {{- with secret "secret/data/services/app2/config" -}}
          {{- range $key, $value := .Data.data }}
            export {{ $key }}={{ $value }}
            {{- end }}
          {{- end -}}
    spec:
      containers:
      - name: app-app2
        image: nginx:latest
        ports:
        - name: web
          containerPort: 80
        resources:
          requests:
            memory: 256Mi
            cpu: 250m
          limits:
            memory: 256Mi
            cpu: 250m
